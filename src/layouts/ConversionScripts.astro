---
/**
 * Add third-party conversion / analytics snippets here.
 * Use is:inline so Astro leaves scripts untouched (required for many vendor trackers).
 *
 * Example:
 * <script is:inline async src="https://www.googletagmanager.com/gtag/js?id=G-XXXX"></script>
 * <script is:inline define:vars={{ id: 'G-XXXX' }}>
 *   window.dataLayer = window.dataLayer || [];
 *   function gtag(){dataLayer.push(arguments);}
 *   gtag('js', new Date());
 *   gtag('config', id);
 * </script>
 */
---
