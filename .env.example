# Required for contact form lead delivery (https://web3forms.com — free tier).
# Copy to .env and set your access key. Keys starting with PUBLIC_ are exposed in the browser (expected for Web3Forms).
PUBLIC_WEB3FORMS_ACCESS_KEY=

# Used for post-submit redirect back to your site (include protocol, no trailing slash).
PUBLIC_SITE_URL=https://www.krest.dental
